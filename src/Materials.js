import * as THREE from "three";

export const mask = new THREE.MeshBasicMaterial({ color: "black" });

export const bloom_layer = new THREE.Layers(2);

export const all_layer = new THREE.Layers(3);
